import { BaseViewModel } from "../base.view-model";

export class RecruiterViewModel extends BaseViewModel{
    public fullName!: string;
    public username!: string;
    public email!: string;
    public phoneNumber!: string;
}